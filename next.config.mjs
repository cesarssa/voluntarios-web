/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com'], // Permite carregar a imagem do favicon do Google
      },
};

export default nextConfig;

